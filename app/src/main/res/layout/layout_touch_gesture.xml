<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <FrameLayout
        android:id="@+id/touch_gesture_layout"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:background="@drawable/shape_touch_gesture"
        android:visibility="gone">

        <TextView
            android:id="@+id/gesture_volume"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_gravity="center"
            android:drawablePadding="5dp"
            android:textColor="#ffffff"
            android:drawableTop="@drawable/ic_volume_up_black_24dp"
            android:visibility="gone"/>

        <TextView
            android:id="@+id/gesture_brightness"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:drawablePadding="5dp"
            android:textColor="#ffffff"
            android:drawableTop="@drawable/ic_brightness_medium_black_24dp"
            android:visibility="gone"/>
    </FrameLayout>
    <TextView
        android:id="@+id/progress_slide"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#ffffff"
        android:text="00:00"
        android:textSize="30dp"
        android:layout_gravity="center"
        android:visibility="gone"/>

</merge>